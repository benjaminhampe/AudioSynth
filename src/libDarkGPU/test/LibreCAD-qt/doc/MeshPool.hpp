#ifndef DE_GPU_MESH_MANAGER_HPP
#define DE_GPU_MESH_MANAGER_HPP

/*
#include <DarkGPU.hpp>
#include <QWidget>

struct MeshPool : public QObject
{
   Q_OBJECT
public:
   MeshPool( QObject* parent );
   ~MeshPool() override;
   bool hasMesh( std::string const & name ) const;
   bool hasMesh( de::gpu::SMesh* mesh ) const;

signals:
   void activatedMesh( de::gpu::SMesh* mesh );
   void changedMesh( de::gpu::SMesh* mesh );
   void removedMesh( std::string name );
public slots:
   //void registerObserver()
   //void registerWatcher();
   //void clearWatchers();

protected:
   void addMesh( de::gpu::SMesh* mesh );
   void removeMesh( std::string name );

   DE_CREATE_LOGGER("MeshPool")
   std::vector< de::gpu::SMesh* > m_Meshes;
};

*/

#endif
